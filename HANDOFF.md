# HANDOFF

The session handoff diary, migration log and ops runbooks live in the private repo `nankolib/opta-ops` (`../opta-ops/HANDOFF.md` when cloned as a sibling). This public tree carries only this stub.
